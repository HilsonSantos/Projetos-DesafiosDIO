IF OBJECT_ID ('DBO.CARGA_VENDAS', 'U') IS NULL
BEGIN
    CREATE TABLE CARGA_VENDAS (
        ID_VENDA INT NOT NULL,
        DATA DATE NOT NULL,
        ID_CLIENTE INT NOT NULL,
        ID_PRODUTO INT NOT NULL,
        ID_VENDEDOR INT NOT NULL,
        QUANTIDADE INT NOT NULL,
        VALOR_BRUTO DECIMAL(18,2) NOT NULL,
        VALOR_DESCONTO DECIMAL(18,2) NOT NULL,
        VALOR_LIQUIDO DECIMAL(18,2) NOT NULL,
        CONSTRAINT PK_ID_CARGA_VENDAS PRIMARY KEY(ID_VENDA)
    )
END